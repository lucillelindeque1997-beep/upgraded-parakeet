import type { Metadata } from "next";
import "./globals.css";
import { Header } from "@/components/header";
import { Footer } from "@/components/footer";

export const metadata: Metadata = {
  title: "AIOS - All In One Solutions | Web Design & Development Agency",
  description: "South African web design and development agency specializing in custom websites, e-commerce solutions, and branding for startups and SMBs. Try before you buy!",
  keywords: "web design, web development, South Africa, e-commerce, branding, AIOS",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className="min-h-screen bg-background font-sans antialiased">
        <div className="flex min-h-screen flex-col">
          <Header />
          <main className="flex-1">
            {children}
          </main>
          <Footer />
        </div>
      </body>
    </html>
  );
}
